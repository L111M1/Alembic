{% set question_types = ["qa", "choice", "judge", "fill"] %}
{% set difficulties = ["beginner", "intermediate", "advanced"] %}
{% set question_type = override_question_type if override_question_type is defined else random.choice(question_types) %}
{% set difficulty = override_difficulty if override_difficulty is defined else random.choice(difficulties) %}
user: Below is a reference sample that has been mutated.

Mutation: {{ mutation }}

Difficulty: {{ difficulty }}
Question type: {{ question_type }}

{{ examples }}

Generate one new SFT training sample by applying a similar mutation to a different topic. Output pure JSON only, no extra text:
{
  "instruction": "new user instruction",
  "output": "corresponding answer"
}
