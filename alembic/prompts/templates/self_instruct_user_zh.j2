{% set question_types = ["问答题", "选择题", "判断题", "填空题"] %}
{% set difficulties = ["入门", "进阶", "高级"] %}
{% set question_type = random.choice(question_types) %}
{% set difficulty = random.choice(difficulties) %}
user: 请自行构思并生成一条全新的 SFT 训练数据。确保与以下已有数据不重复：
难度：{{ difficulty }}
提问类型：{{ question_type }}
{{ existing_instructions }}

请生成一条全新的 SFT 训练数据。用纯 JSON 格式输出，只输出 JSON，不要有其他文字：
{
  "instruction": "新构思的用户指令",
  "output": "对应的高质量回答"
}
