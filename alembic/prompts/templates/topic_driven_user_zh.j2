{% set sub_topics = knowledge | replace("，", ",") | split(",") if knowledge else [] %}
{% if sub_topics and count <= sub_topics|length %}
{% set assigned = sub_topics[:count]|map('trim')|list %}
user: 请为主题 "{{ topic }}" 生成 {{ count }} 条 SFT 训练数据，每条对应一个不同的子主题，必须严格按照以下分配：
{% for st in assigned %}
  第{{ loop.index }}条 → 子主题：{{ st }}
{%- endfor %}
要求：每条数据采用不同的难度（入门/进阶/高级）和不同的提问类型（概念解释、对比分析、应用题、正误判断、简答题、场景模拟、案例分析、推导与证明），任意两条不能有相似的提问角度。
{% else %}
{% set sub_topic_list = sub_topics|map('trim')|join('、') if sub_topics else '' %}
user: 请为主题 "{{ topic }}" 生成 {{ count }} 条多样化的 SFT 训练数据。
{% if sub_topics %}以下子主题每个都必须至少覆盖一次，不能全部集中在某几个子主题上：{{ sub_topic_list }}{% endif %}
{{ count }} 条数据必须涵盖不同的难度（入门、进阶、高级）和提问类型（概念解释、对比分析、应用题、正误判断、简答题、场景模拟、案例分析、推导与证明），任意两条数据的提问角度和结构都不能相似。
{% endif %}
{% if knowledge %}参考以下背景知识来确保准确性：
{{ knowledge }}{% endif %}
用纯 JSON 数组格式输出，只输出 JSON，不要有其他文字：
[
  {
    "instruction": "用户指令",
    "output": "助手回答"
  },
  ...
]
