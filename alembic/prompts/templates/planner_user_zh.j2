{% if existing_angles -%}
{{ existing_angles }}
{% endif -%}
user: 请为主题 "{{ topic }}" 创建 {{ count }} 个互不相同的子主题/提问角度的结构化规划。
{% if knowledge %}背景知识：{{ knowledge }}{% endif %}

每个规划项需要包含：
- "sub_topic"：该主题下的具体子领域
- "angle"：独特的提问视角/角度描述，必须与其他所有规划项明显不同
- "difficulty"：入门、进阶 或 高级
- "question_type"：概念解释、对比分析、应用题、正误判断、简答题、场景模拟、案例分析、推导与证明

重要：确保所有 {{ count }} 个角度互不相同。任意两项不能有相似的提问模式、视角或结构。每个角度应从完全不同的方向来探讨该主题。

用纯 JSON 数组格式输出，只输出 JSON，不要有其他文字：
[
  {
    "sub_topic": "子领域名称",
    "angle": "独特的视角描述",
    "difficulty": "进阶",
    "question_type": "概念解释"
  },
  ...
]
